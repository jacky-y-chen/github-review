# GitHub Year in Review 🎬

一个现代化的 Web 应用，用于生成你的 GitHub 年度总结视频。输入你的 GitHub 用户名和 Personal Access Token，即可获取你的年度贡献数据并生成精美的可视化视频。

## ✨ 功能特性

- 📊 **数据可视化**: 展示提交数、PR、Issues、语言分布等统计数据
- 🤖 **AI 智能总结**: 使用魔搭社区 DeepSeek-V3.2 为你的 Top 3 项目生成智能总结
- 🎨 **科技极简风格**: 深色主题，流畅动画，符合 GitHub 品牌调性
- 📱 **响应式设计**: 完美支持移动端和桌面端
- 🔒 **隐私安全**: Token 仅存储在浏览器 sessionStorage，不上传服务器
- ⚡ **双模式渲染**: 支持快速预览(720p/60fps)和高清导出(1080p/60fps)
- ⏱️ **灵活时间范围**: 支持 3 个月、6 个月、1 年或全部时间

## 🎥 视频内容

生成的视频包含以下章节（约 25-30 秒，根据数据动态调整）：

1. **开场动画** (0-3s): 用户头像、用户名展示
2. **统计数据** (3-8s): 总提交数、PR 数、Issue 数
3. **语言分布** (8-13s): 开发语言占比柱状图
4. **活跃仓库** (13-18s): Top 5 最活跃的仓库
5. **贡献热力图** (18-23s): 年度贡献日历（如果有数据）
6. **AI 项目总结** (23-28s): Top 3 项目智能分析
7. **结尾致谢** (最后 3s): 感谢页面

💡 **提示**: 视频采用炫酷的转场特效，让画面切换更加流畅自然。如果没有贡献热力图数据，该场景会自动跳过。

## 🚀 快速开始

### 前置要求

- Node.js 18+
- npm 或 pnpm

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 `http://localhost:5173` 查看应用。

### 构建生产版本

```bash
npm run build
npm run preview
```

## 🔑 获取 GitHub Personal Access Token

1. 访问 GitHub Settings → Developer settings → [Personal access tokens](https://github.com/settings/tokens) → Tokens (classic)
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 设置 Token 名称和过期时间
4. 选择以下权限（scopes）:
   - ✅ `repo` - 完整的仓库访问权限
   - ✅ `read:user` - 读取用户信息
5. 生成并复制 Token（妥善保管，只会显示一次）

⚠️ **安全提示**: Token 具有访问你 GitHub 账户的权限，请勿分享给他人。本应用仅在浏览器本地使用 Token，不会上传到任何服务器。

## 🤖 获取魔搭社区 API Key（可选）

如果希望使用 AI 智能总结功能，需要获取魔搭社区 API Key：

1. 访问 [魔搭社区](https://www.modelscope.cn)
2. 注册/登录账号
3. 访问 [API Token 管理页面](https://www.modelscope.cn/my/myaccesstoken)
4. 创建新的 API Key 并复制
5. 在应用中输入 API Key，即可使用 DeepSeek-V3 模型生成项目总结

💡 **提示**: 不输入 API Key 也可以正常生成视频，只是会缺少 AI 总结部分。

## 📁 项目结构

```
githubtool/
├── src/
│   ├── components/          # React 组件
│   │   ├── InputForm.tsx    # 用户输入表单
│   │   ├── VideoPreview.tsx # 视频预览组件
│   │   └── StatsDisplay.tsx # 统计数据展示页
│   ├── remotion/            # Remotion 视频组件
│   │   ├── scenes/          # 视频场景
│   │   │   ├── OpeningScene.tsx
│   │   │   ├── StatsScene.tsx
│   │   │   ├── LanguageScene.tsx
│   │   │   ├── RepositoriesScene.tsx
│   │   │   ├── ContributionScene.tsx
│   │   │   ├── SummaryScene.tsx      # AI 总结场景
│   │   │   └── EndingScene.tsx
│   │   ├── components/      # 视频公共组件
│   │   │   ├── StarBackground.tsx    # 星空背景
│   │   │   ├── TransitionWrapper.tsx # 转场动画
│   │   │   └── CameraLayout.tsx      # 镜头运动
│   │   ├── GitHubYearVideo.tsx
│   │   └── index.ts
│   ├── services/            # 服务层
│   │   ├── githubApi.ts     # GitHub API 调用
│   │   ├── modelscopeLLM.ts # 魔搭 AI 服务
│   │   ├── videoRenderer.ts # 视频渲染
│   │   └── webpackOverride.ts
│   ├── store/               # 状态管理
│   │   └── useStore.ts      # Zustand store
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── README.md
```

## 🛠️ 技术栈

- **前端框架**: React 18 + TypeScript
- **构建工具**: Vite 5
- **样式**: Tailwind CSS 3
- **状态管理**: Zustand 4
- **视频生成**: Remotion 4
- **API 调用**: Octokit GraphQL, 魔搭社区 API
- **AI 模型**: DeepSeek-V3（魔搭社区）
- **动画**: Remotion Spring & Interpolate

## 📦 视频导出

### 浏览器端导出（简化版）

点击"导出视频"按钮，应用会导出 JSON 数据文件。

### 命令行导出（推荐 - 高质量）

要生成真实的 MP4 视频文件，请使用 Remotion CLI：

```bash
# 安装 Remotion CLI（如果未安装）
npm install

# 渲染视频（需要先获取数据）
npx remotion render src/remotion/index.ts GitHubYearVideo out/video.mp4

# 预览视频（在浏览器中）
npx remotion preview src/remotion/index.ts
```

### 服务器端渲染

对于生产环境，推荐使用：

- [Remotion Lambda](https://www.remotion.dev/docs/lambda) - AWS Lambda 渲染
- [Remotion Server](https://www.remotion.dev/docs/server) - 自建渲染服务器

## 🎨 自定义配置

### 修改视频尺寸

编辑 [src/remotion/index.ts](src/remotion/index.ts):

```typescript
<Composition
  width={1920} // 修改宽度
  height={1080} // 修改高度
  fps={60} // 修改帧率
/>
```

### 修改配色

编辑 [tailwind.config.js](tailwind.config.js):

```javascript
colors: {
  github: {
    green: '#2ea44f',  // 主色调
    blue: '#58a6ff',   // 强调色
    // ...
  }
}
```

### 修改视频时长

编辑 [src/remotion/GitHubYearVideo.tsx](src/remotion/GitHubYearVideo.tsx)，调整各场景的 `startFrame` 和 `endFrame`。

## 🔧 故障排除

### Token 无效

- 确保 Token 包含正确的权限（`repo` 和 `read:user`）
- 检查 Token 是否过期
- 确认用户名拼写正确

### API 请求失败

- GitHub API 有速率限制（认证用户 5000 次/小时）
- 检查网络连接
- 确认 GitHub 服务状态

### 视频渲染慢

- 使用"预览模式"可加快速度
- 浏览器端渲染性能有限，推荐使用 CLI 渲染
- 关闭其他占用资源的应用

## 📄 开源协议

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📮 联系方式

如有问题或建议，请通过 GitHub Issues 联系。

---

⭐ 如果这个项目对你有帮助，请给一个 Star！

Made with ❤️ and Remotion
